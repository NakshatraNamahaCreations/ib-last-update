import React from "react";
import "./PageLoader.css"; // Create a CSS file for styling your loader

function PageLoader() {
  return (
    <div className="page-loader">
      <div className="loader"></div>
    </div>
  );
}

export default PageLoader;
